import java.awt.*;

/**
 * Contains all the details and parameters of the animation.
 */
class Properties {

    static int WIDTH = 1920;
    static int HEIGHT = WIDTH / 16 * 9;

    static int POINTS = 10000;
    static int SIZE = 1;

    static Color BACK_COLOR = new Color(192, 57, 43);
    static Color COLOR = new Color(236, 240, 241);

}